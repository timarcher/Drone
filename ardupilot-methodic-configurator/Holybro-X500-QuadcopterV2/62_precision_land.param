# Precision landing disabled. The X500 QuadcopterV2 has no IR beacon or precision landing
# camera installed. PLND_ENABLED=0 is the ArduPilot firmware default — no param change needed.
# LAND_SPEED and LAND_ALT_LOW are set in 16_safety_setup.param.
