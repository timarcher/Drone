BRD_HEAT_LOWMGN,0  # Now that IMU temperature is calibrated, arm ASAP
BRD_HEAT_TARG,45  # Reset to default after offline IMU temperature calibration
LOG_DISARMED,0  # IMU temperature calibration is finished, we no longer need to log data when disarmed
